This project is based on self-evolving agents. Current AI agents can work on given prompt but cannot change thier own 
architecture according to the needs. This project tries to solve that issue by allowing the agents to improve their own 
architecture, not just outputs.
Idea

An agent that:
rewrites its own prompts
modifies its tool usage strategy
fine-tunes small internal modules (like adapters or policies)

